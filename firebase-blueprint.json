{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores progress and data for each user.",
      "type": "object",
      "properties": {
        "completed_lessons": {
          "type": "array",
          "description": "List of IDs of completed lessons."
        },
        "exam_scores": {
          "type": "object",
          "description": "Map of exam IDs to scores."
        },
        "total_progress": {
          "type": "number",
          "description": "Overall progress percentage."
        },
        "level": {
          "type": "string",
          "description": "User's current proficiency level (A1, A2)."
        },
        "flashcard_points": {
          "type": "number",
          "description": "Durable reward points earned by completing key daily flashcard sessions."
        },
        "flashcard_streak": {
          "type": "number",
          "description": "Active streak of completed daily flashcard challenges."
        },
        "last_flashcard_date": {
          "type": "string",
          "description": "Date of the last completed daily flashcard challenge (such as YYYY-MM-DD)."
        },
        "flashcard_selected_limit": {
          "type": "number",
          "description": "User's chosen daily flashcard limit (e.g. 3, 5, 10)."
        },
        "updated_at": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["completed_lessons", "exam_scores", "total_progress", "level"]
    }
  },
  "firestore": {
    "/profiles/{userId}": {
      "schema": "UserProfile",
      "description": "Primary progress tracking for a user."
    }
  }
}
